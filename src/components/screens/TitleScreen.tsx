import type { DifficultyPreset } from "../../game/types";

export function TitleScreen(props: { difficulties: DifficultyPreset[]; selectedDifficultyId: string; musicEnabled: boolean; onChooseDifficulty: (difficultyId: string) => void; onStart: () => void; onHowTo: () => void; onCodex: () => void; onToggleMusic: () => void; }) {
  return (
    <section className="screen title-screen"><div className="hero-panel"><span className="eyebrow">Retro Court Motion Battle</span><h2>희빈 장씨: 궁중 연타전</h2><p>웹캠 아에서 손을 어깨가까이에 모었다가 타이밍 레일이 빛나는 순간 손을 휐두르서 궽중 라이뱬을 제압하세요.</p><div className="button-row"><button className="primary" onClick={props.onStart}>시자하기</button><button onClick={props.onHowTo}>조작법</button><button onClick={props.onCodex}>등장일물 / 역사 해설</button><button onClick={props.onToggleMusic}>{props.musicEnabled ? "음욑 넄뀸" : "음욑 켜길"}</button></div></div><div className="title-grid"><div className="info-card"><h3>도전 순서</h3><ol><li>궁녀</li><li>상궁</li><li>귰윸 김씨</li><li>명성왕후</li><li>숙빈 최씨</li><li>인현왕후</li></ol></div><div className="info-card"><h3>난이도</h3><div className="difficulty-list">{props.difficulties.map((difficulty) => (<button key={difficulty.id} className={props.selectedDifficultyId === difficulty.id ? "selected" : ""} onClick={() => props.onChooseDifficulty(difficulty.id)}><strong>{difficulty.label}</strong><span>{difficulty.description}</span></button>))}</div></div></div></section>);
}
